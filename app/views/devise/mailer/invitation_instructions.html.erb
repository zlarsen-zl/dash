<p><%= t("devise.mailer.invitation_instructions.hello", email: @resource.name) %></p>

<p>
  <% #TODO: UPDATE THE URL %>
  <%= t("devise.mailer.invitation_instructions.someone_invited_you", url: 'Localhost:3000') %>
</p>

<p>
  <%= link_to t("devise.mailer.invitation_instructions.accept"), 
  "http://localhost:3000/invitation/accept?token=#{@token}" %>
</p>

<% if @resource.invitation_due_at %>
  <p>
    <%= t("devise.mailer.invitation_instructions.accept_until", 
      due_date: l(@resource.invitation_due_at, 
      format: :'devise.mailer.invitation_instructions.accept_until_format')) 
    %>
  </p>
<% end %>

<p><%= t("devise.mailer.invitation_instructions.ignore").html_safe %></p>
